{
    "version": 1,
    "builds": [
        {
            "src": "server.js",
            "use": "@vercel/node"
        },
        {
            "src": "server.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
 
        {
            "src": "/user(.*)",
            "dest": "/server.js"
        },
        {
            "src": "/message(.*)",
            "dest": "/server.js"
        }
    ]
}